<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="room"
            type="mapComponents.Room" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".CreateActivity">

        <EditText
            android:id="@+id/nameInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="116dp"
            android:hint="@string/introduce_room_name"
            android:inputType="text"
            android:text="@{room.name}"
            android:minHeight="48dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <EditText
            android:id="@+id/roomXWidth"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="128dp"
            android:hint="introduce x"
            android:inputType="text"
            android:minHeight="48dp"
            android:text="@={room.width}"
            app:layout_constraintTop_toBottomOf="@+id/nameInput"
            tools:layout_editor_absoluteX="0dp" />

        <EditText
            android:id="@+id/roomYLength"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:hint="introduce y"
            android:inputType="text"
            android:minHeight="48dp"
            android:text="@={room.length}"
            app:layout_constraintTop_toBottomOf="@+id/roomXWidth"
            tools:layout_editor_absoluteX="0dp" />

        <Button
            android:id="@+id/next"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="NEXT"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/roomYLength" />

        <TextView
            android:id="@+id/textView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Position yourself in one of the room's corners. Measure the steps it takes to arrive to the next corner. Repeat with the following wall to obtain an approximation of the room's dimensions."
            app:layout_constraintBottom_toTopOf="@+id/roomXWidth"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/nameInput" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
